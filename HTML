<<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brain Lesion Detection</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Your CSS code here */
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }
        h1 {
            color: #333;
        }
        form {
            margin-top: 20px;
        }
        input[type="file"] {
            margin-right: 10px;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        #predictionResult {
            margin-top: 20px;
            display: none;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Brain Lesion Detection</h1>
        <form id="uploadForm" enctype="multipart/form-data">
            <input type="file" id="imageUpload" accept=".jpg,.jpeg,.png" required>
            <button type="submit">Upload Image</button>
        </form>
        <div id="predictionResult">
            <h2>Prediction Result:</h2>
            <!-- Prediction result will be displayed here -->
        </div>
    </div>

    <script>
        document.getElementById('uploadForm').addEventListener('submit', async (event) => {
            event.preventDefault(); // Prevent default form submission
            const formData = new FormData();
            formData.append('file', document.getElementById('imageUpload').files[0]);

            try {
                const response = await fetch('/predict', {
                    method: 'POST',
                    body: formData
                });

                if (!response.ok) {
                    throw new Error('Failed to upload image');
                }

                const result = await response.json();
                displayPredictionResult(result);
            } catch (error) {
                console.error(error);
            }
        });

        function displayPredictionResult(result) {
            const predictionResult = document.getElementById('predictionResult');
            predictionResult.style.display = 'block';

            // Assuming the result contains the predictions in JSON format
            // Modify this part to display the predictions according to your model's output
            const predictions = result.predictions;
            const predictionList = document.createElement('ul');
            predictions.forEach((prediction) => {
                const listItem = document.createElement('li');
                listItem.textContent = prediction;
                predictionList.appendChild(listItem);
            });
            predictionResult.appendChild(predictionList);
        }
    </script>
</body>
</html>
